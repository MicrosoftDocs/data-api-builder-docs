# Dab Developer (agent resources)

This folder is the isolated reference set for the `dab-developer` agent.

- Start here: `resources.md`
- Scope: DAB CLI workflows (init/configure/add/update/validate/start/export), config schema usage, relationships, auth, and SQL MCP Server.
